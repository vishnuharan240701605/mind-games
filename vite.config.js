import { defineConfig } from 'vite';

export default defineConfig({
    // Base path — set to '/' for custom domains or username.github.io repos
    // Set to '/<repo-name>/' if deploying to https://<user>.github.io/<repo>/
    base: './',
});
